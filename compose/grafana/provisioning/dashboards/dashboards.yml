apiVersion: 1

providers:
  - name: app-dashboards
    folder: TodoApp
    type: file
    disableDeletion: false
    allowUiUpdates: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

